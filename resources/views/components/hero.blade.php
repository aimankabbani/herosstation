<section class="hero">
    <div class="container">
        <span class="hero-phone">{{$phoneNumber}}</span>
        <h1>
            <span class="highlight">{{$title}}</span>
        </h1>
        <p class="hero-subtitle">
            <!-- add Slogen -->
            {{$slogan}}
        </p>
    </div>
</section>